<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
 <style>
        body {
            font-family: 'Arial', sans-serif;
        }

        .container {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
        }
        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #007bff;
        }

        button[type="submit"] {
            background-color: #28a745;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
        }

        button[type="submit"]:hover {
            background-color: #418838;
        }
         button[type="reset"] {
            background-color: #28a745;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
            margin-left: 650px;
        }

        button[type="reset"]:hover {
            background-color: #218838;
        }

        .btn-primary {
            margin-top: 10px;
        }

        .btn-primary a {
            color: #fff;
            text-decoration: none;
        }

        .btn-primary:hover {
            background-color: #007bff;
        }
    </style>
    <title>Document</title>
</head>
<body>
      <div class="container col-6 mt-5 ">
        <form class="row g-3 d-flex justify-content-center" action="/sua/{{item.id}}" method="post">
            <h2 class="d-flex justify-content-center">Sửa Sản Phẩm</h2>
            <div class="col-md-12">
                <label for="inputEmail4" class="form-label">Tên Sản Phẩm</label></label>
                <input type="text" class="form-control" name="ten_sp" value="{{item.ten_sp}}" id="inputEmail4">
            </div>
             <div class="col-md-12">
                <label for="inputEmail4" class="form-label">Giá Sản Phẩm</label></label>
                <input type="number" class="form-control" name="gia_tien" value="{{item.gia_tien}}" id="inputEmail4">
            </div>
             <div class="col-md-12">
                <label for="inputEmail4" class="form-label">Số Lượng</label></label>
                <input type="number" class="form-control" name="so_luong" value="{{item.so_luong}}" id="inputEmail4">
            </div>
             <div class="mb-3">
                <label for="exampleFormControlInput1" class="form-label">Mô tả</label>
                <textarea type="text" class="form-control"
                id="exampleFormControlInput1" name="mo_ta"  placeholder="Mô tả sản phẩm" 
                required>{{item.mo_ta}}</textarea>
            </div>
            <a>
                <button type="submit">Sửa Sản Phẩm</button>
                </a>
        </form>
          <a href="/"><button type="reset" class="">Quay Lại</button></a>


        </div>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>